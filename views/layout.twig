<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="/styles/header.css">
    <link rel="stylesheet" href="/styles/base.css">
    <title>Your Crypto Bro</title>

</head>
<body>

<div class="header">
    <div>
        <h1 class="logo">
            <a class="logo-text" href="/">YOUR CRYPTO BRO</a>
        </h1>
    </div>

    <div class="search-div">
        <form action="/" method="post">
            <label for="search">Search crypto: </label>
            <input class="search-bar" type="text" id="search" name="search" placeholder="COIN">
            <input type="submit" value="Search">
        </form>
    </div>


    <div class="header-profile">
        {% if auth %}
            <div>
                <p class="welcome">Welcome {{ auth.name }}</p>
            </div>
            <div>
                <form action="/profile" method="get">
                    <button class="left-side-buttons">Profile</button>
                </form>
                <form action="/logout" method="get">
                    <button class="left-side-buttons">Logout</button>
                </form>
            </div>
        {% else %}
            <form action="/login" method="get">
                <button class="left-side-buttons">Login</button>
            </form>
        {% endif %}
    </div>
</div>

{% block content %}{% endblock %}
</body>
</html>